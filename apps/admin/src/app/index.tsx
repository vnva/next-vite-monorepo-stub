import { Button } from '@packages/ui-kit';
import './styles/index.scss';

export const App = () => {
  return (
    <main>
      <h1>Admin app</h1>
      <Button>Button from UI Kit package</Button>
    </main>
  );
};
